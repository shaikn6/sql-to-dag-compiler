# ============================================================
# AUTO-GENERATED BY sql-to-dag-compiler
# Source: {{ source_file }}
# Generated: {{ generated_at }}
# DO NOT EDIT — re-run the compiler to regenerate.
# ============================================================

from datetime import datetime, timedelta

from airflow import DAG
from airflow.operators.python import PythonOperator

# ---------------------------------------------------------------------------
# Default arguments applied to every task
# ---------------------------------------------------------------------------
default_args = {
    "owner": "{{ dag_owner }}",
    "depends_on_past": False,
    "email_on_failure": False,
    "email_on_retry": False,
    "retries": {{ retries }},
    "retry_delay": timedelta(minutes={{ retry_delay_minutes }}),
}

# ---------------------------------------------------------------------------
# SQL statements — keyed by task id
# ---------------------------------------------------------------------------
SQL_STATEMENTS = {
{% for task in tasks %}
    "{{ task.task_id }}": """
{{ task.sql | indent(4, first=True) }}
""",
{% endfor %}
}


def execute_sql(task_id: str, **context) -> None:
    """
    Execute the SQL statement for *task_id*.

    In production, replace this stub with your Redshift/PostgreSQL hook:

        from airflow.providers.amazon.aws.hooks.redshift_sql import RedshiftSQLHook
        hook = RedshiftSQLHook(redshift_conn_id="redshift_default")
        hook.run(SQL_STATEMENTS[task_id])
    """
    sql = SQL_STATEMENTS[task_id]
    print(f"[{task_id}] Executing SQL ({len(sql)} chars):")
    print(sql[:200] + ("..." if len(sql) > 200 else ""))
    # TODO: wire to RedshiftSQLHook / PostgresHook


# ---------------------------------------------------------------------------
# DAG definition
# ---------------------------------------------------------------------------
with DAG(
    dag_id="{{ dag_id }}",
    default_args=default_args,
    description="{{ dag_description }}",
    schedule_interval="{{ schedule_interval }}",
    start_date=datetime({{ start_date_year }}, {{ start_date_month }}, {{ start_date_day }}),
    catchup=False,
    tags={{ tags | tojson }},
) as dag:

{% for task in tasks %}
    {{ task.task_id }} = PythonOperator(
        task_id="{{ task.task_id }}",
        python_callable=execute_sql,
        op_kwargs={"task_id": "{{ task.task_id }}"},
        doc_md="""
**Statement type:** {{ task.statement_type }}
**Target table:** {{ task.target_table or "N/A" }}
**Source tables:** {{ task.source_tables | join(", ") or "N/A" }}
**Aggregations:** {{ task.aggregations | join(", ") or "none" }}
**Has WHERE clause:** {{ task.has_where }}
**Has GROUP BY:** {{ task.has_group_by }}
        """,
    )
{% endfor %}

    # ---------------------------------------------------------------------------
    # Task dependencies (derived from table-level lineage)
    # ---------------------------------------------------------------------------
{% if dependency_lines %}
{% for line in dependency_lines %}
    {{ line }}
{% endfor %}
{% else %}
    # No inter-statement dependencies detected — all tasks are independent.
{% endif %}
